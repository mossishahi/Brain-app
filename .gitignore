node_modules/
packages/*/dist/
apps/*/dist/
apps/*/dist-test/
*.tsbuildinfo
# written by setup.sh and deploy/slurm-launch.sh after a successful build
.build-stamp
.DS_Store
.env
.env.*
.brainstorm-agentic/
coverage/
*.log

# SLURM wrapper job logs (logs/ itself is git-kept)
/logs/*.log

# proof of what the test suites actually executed, written by the server suite
# and read by brain/scripts/publish-bundle.mjs as its publish gate
.test-bundle-used.json
