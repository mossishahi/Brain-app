{
  "name": "@brainstorm-agentic/executor-cursor-agent",
  "version": "0.1.0",
  "description": "Cursor SDK AgentExecutor authenticated with a Cursor API key.",
  "type": "module",
  "main": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "import": "./dist/src/index.js"
    }
  },
  "files": [
    "dist/src"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test dist/test/*.test.js"
  },
  "dependencies": {
    "@brainstorm-agentic/credit-recovery": "0.1.0",
    "@brainstorm-agentic/core": "0.1.0",
    "@brainstorm-agentic/host-tools": "0.1.0",
    "@cursor/sdk": "^1.0.28"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.6.0"
  }
}
